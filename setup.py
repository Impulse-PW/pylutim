import os
import setuptools

setuptools.setup(
    name="pylutim",
    version="0.5",
    description="API wrapper for Lutim (Let's Upload That IMage)",
    install_requires=["validators"],
    author="Impulse-PW",
    url="none",
    author_email="impulse-pw@openmailbox.org",
    packages=["pylutim", "pylutim.lib"],
    license="GPLv3",
    keywords="pylutm, lutim, image, upload, api, warapper"
    )
